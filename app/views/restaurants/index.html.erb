<h1>All Restaurants</h1>


<% @restaurants.each do |restaurant| %>
<ul>
  <li><%= link_to "#{restaurant.name} - #{restaurant.address}", restaurant_path(restaurant)  %></li>
  <li> <%= image_tag restaurant.image.url(:thumb)  %> </li>
</ul>
<% end %>
